import enum
from math import prod


class Solution:
    def productExceptSelf(self, nums: List[int]) -> List[int]:
        total = prod(nums)
        for i, num in range(1, len(nums)):
            result[i] = 


